CREATE TABLE sponsorTable(
    id TEXT NOT NULL PRIMARY KEY,
    description TEXT
);

insertUpdate:
INSERT OR REPLACE INTO  sponsorTable(id, description)
VALUES (?, ?);

selectAll:
SELECT * FROM sponsorTable;

sponsorById:
SELECT * FROM sponsorTable WHERE id = ? LIMIT 1;

deleteAll:
DELETE FROM sponsorTable;