
apply plugin: 'konan'

konan.targets = [/*'iphone',*/ 'iphone_sim']

konanArtifacts {
    framework('SessionizeArch') {
        dependencies {
            /*artifactSessionizeArch deps.knarch.dbNative
            artifactSessionizeArch deps.sqldelight.runtimeNative*/
//            artifactSessionizeArch deps.knarch.threadsNative
            artifactSessionizeArch deps.sqldelight.multiplatformdriverNative
            artifactSessionizeArch "com.russhwolf:library:0.1-alpha4"
        }

        linkerOpts "-lsqlite3"
        enableMultiplatform true
    }
}

dependencies {
    expectedBy project(':sessionize')
}