<?xml version="1.0" encoding="utf-8"?>
<manifest package="co.touchlab.sessionize"
          xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="co.touchlab.sessionize.MainApp"
        android:theme="@style/Theme.Shrine">
        <activity
            android:name="co.touchlab.sessionize.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
                android:name="io.fabric.ApiKey"
                android:value="90b5b9675372e1f2aff36d43ea5d6869040d37f3"
                />

        <receiver android:name="co.touchlab.sessionize.NotificationsApiImpl$NotificationPublisher"  android:exported="true">
            <intent-filter>
                <action android:name="NotificationAction"/>
            </intent-filter>
        </receiver>


        <service
                android:name=".FirebaseMessageHandler"
                android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>