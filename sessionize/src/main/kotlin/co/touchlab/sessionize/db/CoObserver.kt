package co.touchlab.sessionize.db

/*
import co.touchlab.multiplatform.architecture.livedata.Observer
import co.touchlab.sessionize.platform.MainDispatcher
import kotlinx.coroutines.experimental.launch

class CoObserver<T>(val proc:*/
/*suspend *//*
(t: T)->Unit):Observer<T>{
    override fun onChanged(t: T?) {
        if(t != null)
            launch(MainDispatcher) { proc(t) }
    }
}
*/
